import { TextInput } from '@beam/ui';
import { PropsTable } from '../../components/docs/PropsTable';

export const CircleIcon = () => <span className="block size-3 rounded-full border border-current" />;

<PageHeader
  title="Input"
  description="The complete leading-icon, value, and trailing-action input used throughout Beam-App-Project."
/>

## States

<PreviewCard compact>
  <div className="flex items-start gap-8">
    {[
      ['Default', 'default', 'Placeholder'],
      ['Focused', 'focused', 'Placeholder'],
      ['Filled', 'filled', 'Input value'],
      ['Disabled', 'disabled', 'Placeholder'],
      ['Error', 'error', 'Placeholder'],
    ].map(([label, state, copy]) => (
      <div className="flex h-[52px] w-[144px] flex-col items-center gap-2" key={state}>
        <TextInput
          visualState={state}
          placeholder={copy}
          value={state === 'filled' ? copy : undefined}
          leading={<CircleIcon />}
          trailing={<CircleIcon />}
          aria-label={`${label} input`}
        />
        <span className="text-xs leading-[18px] text-[rgb(var(--color-gray-600))]">{label}</span>
      </div>
    ))}
  </div>
</PreviewCard>

## Specification

<PropsTable compact>
  <tbody>
    <tr><th>Height</th><td>26px</td></tr>
    <tr><th>Padding</th><td>4px 8px</td></tr>
    <tr><th>Content gap</th><td>6px</td></tr>
    <tr><th>Icon</th><td>12px</td></tr>
    <tr><th>Radius</th><td>4px</td></tr>
    <tr><th>Typography</th><td>Inter · 12px / 18px · Regular</td></tr>
  </tbody>
</PropsTable>
