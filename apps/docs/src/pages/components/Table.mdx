import { Table, TableHeader, TableRow } from '@beam/ui';
import { PropsTable } from '../../components/docs/PropsTable';

export const CircleIcon = () => <span className="block size-3 rounded-full border border-current" />;

<PageHeader title="Table" description="Header and row building blocks composed into the complete Beam-App-Project table." />

## Header

<PreviewCard compact white>
  <div className="w-full max-w-[620px]">
    <TableHeader labels={['Name', 'Size', 'Modified']} className="rounded-[4px]" />
  </div>
</PreviewCard>

## Row states

<PreviewCard compact tall>
  <div className="flex w-full max-w-[620px] flex-col gap-2">
    {[
      ['Default', 'default'],
      ['Hover', 'hover'],
      ['Selected', 'selected'],
      ['Disabled', 'disabled'],
    ].map(([label, state]) => (
      <div className="grid grid-cols-[1fr_64px] items-center gap-4" key={state}>
        <TableRow
          standalone
          name="Table row"
          size="2.4KB"
          modified="5 days ago"
          state={state}
          actionIcon={<CircleIcon />}
        />
        <span className="text-xs leading-[18px] text-[rgb(var(--color-gray-600))]">{label}</span>
      </div>
    ))}
  </div>
</PreviewCard>

## Complete table

<PreviewCard compact tall>
  <div className="w-full max-w-[620px]">
    <Table
      rows={[
        { name: 'Folder 001', size: '2.4KB', modified: '5 days ago' },
        { name: 'Product Resources', size: '856MB', modified: 'Yesterday' },
        { name: 'Website Assets', size: '856MB', modified: '2 hours ago' },
      ]}
      actionIcon={<CircleIcon />}
    />
  </div>
</PreviewCard>

## Specification

<PropsTable compact>
  <tbody>
    <tr><th>Types</th><td>Header and row</td></tr>
    <tr><th>Header items</th><td>Name, Size, Modified</td></tr>
    <tr><th>Header height</th><td>26px</td></tr>
    <tr><th>Row height</th><td>26px</td></tr>
    <tr><th>Cell padding</th><td>4px 8px</td></tr>
    <tr><th>Trailing action</th><td>26px button with 12px circle icon</td></tr>
    <tr><th>Radius</th><td>4px</td></tr>
    <tr><th>Typography</th><td>Inter · 12px / 18px · Regular</td></tr>
  </tbody>
</PropsTable>
