<PageHeader
  title="Getting Started"
  description="This design system packages the Beam tokens and React components: token primitives, a Tailwind preset, and components. Light/dark and accessible by default."
/>

<div className="getting-started-guide">
  <div className="getting-started-row">
    <div className="getting-started-label">Install</div>
    <div className="getting-started-panel">

```bash
pnpm install
pnpm dev   # docs at http://localhost:5174
```

    </div>
  </div>

  <div className="getting-started-row">
    <div className="getting-started-label">Use</div>
    <div className="getting-started-panel">

```tsx
import { Button } from '@beam/ui';
import '@beam/tokens/theme.css';

<Button>Click me</Button>
```

    </div>
  </div>

  <div className="getting-started-row">
    <div className="getting-started-label">Wire the Tailwind preset in your app's tailwind.config.ts.</div>
    <div className="getting-started-panel">

```ts
import preset from '@beam/tokens/tailwind-preset';

export default {
  presets: [preset],
  content: ['./index.html', './src/**/*.{ts,tsx,mdx}'],
};
```

    </div>
  </div>
</div>

Both packages are local workspace packages in this repo: `packages/tokens` (published name `@beam/tokens`) and `packages/ui` (`@beam/ui`).

## How tokens work

Two layers, bridged by Tailwind:

- Primitives (`packages/tokens/src/primitives.ts`) — raw color scales as `"R G B"` triples for alpha composition.
- Semantic (`packages/tokens/src/theme.css`) — what components consume: `bg-canvas`, `text-primary`, `accent.*`. Two scopes: `:root` (light) and `.dark`.
- Preset (`packages/tokens/src/tailwind-preset.ts`) — every semantic variable becomes a Tailwind utility (`bg-canvas`, `border-default`, …).

The token values were collected from the Beam apps on 2026-07-21 and are this repo's token source — see the Foundations pages for the full scales.

Components never read tokens directly. They consume utility classes only.

## Component conventions

- TypeScript + React 18, `forwardRef`, semicolons, plain exports.
- Classes composed via `cn()` (clsx + tailwind-merge) so consumers can override with `className` safely.

#### What's shipped

- Components — [Button](/components/button), [Input](/components/text-input), [Select](/components/select), [Menu](/components/menu), [Badge](/components/badge), [Table](/components/table), [File Card](/components/file-card), [Dialog](/components/dialog), [Popover](/components/popover), [Editor Toolbar](/components/editor-toolbar), [Activity](/components/activity), [Tooltip](/components/tooltip), [Toast](/components/toast), [Navigation](/components/navigation), [Tab](/components/tabs).
- Foundations — [Color](/foundations/color), [Typography](/foundations/typography), [Spacing](/foundations/spacing), [Radius](/foundations/radius), [Elevation](/foundations/elevation).
