{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "description": "TEMPLATE for real client repos consuming beam-* packages. Copy this file to your repo root and adjust the schedule/auto-merge rules to your team's taste.",
  "packageRules": [
    {
      "description": "Beam Design System packages — group + frequent",
      "matchPackageNames": [
        "@beam/ui",
        "@beam/tokens"
      ],
      "groupName": "Beam Design System",
      "schedule": ["before 9am on monday"],
      "labels": ["beam-design-system", "automated"],
      "commitMessagePrefix": "deps(beam):",
      "automerge": false,
      "minimumReleaseAge": "1 day"
    },
    {
      "description": "Auto-merge patch updates from beam-* if CI passes",
      "matchPackageNames": [
        "@beam/ui",
        "@beam/tokens"
      ],
      "matchUpdateTypes": ["patch"],
      "automerge": true,
      "automergeType": "pr",
      "platformAutomerge": true
    }
  ],
  "vulnerabilityAlerts": {
    "enabled": true,
    "labels": ["security"]
  }
}
